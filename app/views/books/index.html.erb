<h2>Books</h2>
<table>
  <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Opinion</th>
    </tr>
  </thead>
  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= image_tag 'sample-author1.jpg' %></td>
      <%= link_to book_path(book.id) do %>
        <td><%= book.title %></td>
      <% end %>
        <td><%= book.body %></td>
      </tr>
    <% end %>
  </tbody>
</table>
